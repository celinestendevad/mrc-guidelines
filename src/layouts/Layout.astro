---
import "../styles/global.css";
import SidebarNav from "../components/SidebarNav.astro";
import Footer from "../components/Footer.astro";

const {
  title = "MRC Brand Guidelines",
  description = "Official brand guidelines for Mikkeller Running Club. Learn how to use logo, colours, typography, graphics and social media consistently across chapters worldwide.",
  currentPage = "",
} = Astro.props;
---


<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
  </head>

  <body style="margin: 0; background: #fff;">
    <div
      style="
        display: flex;
        min-height: 100vh;
        max-width: 1600px;
        margin: 0 auto;
      "
    >
      <!-- Venstre: sticky sidebar -->
      <SidebarNav currentPage={currentPage} />

      <!-- Højre: indhold + footer -->
      <div style="flex: 1; display: flex; flex-direction: column;">
        <main style="flex: 1;">
          <slot />
        </main>

        <Footer />
      </div>
    </div>
  </body>
</html>
