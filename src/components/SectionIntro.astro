sectionintro

---
const { text = "" } = Astro.props;
---

<style>
  .section-intro {
    margin-top: 96px; 
  }

  .section-intro__heading {
    text-transform: uppercase;
    margin: 0;
  }

  /* STREGEN – skal gå kant til kant */
  .section-intro__line {
    margin-top: 96px;
    height: 1px;
    width: calc(100% + 200px);     /* 80px + 80px */
    margin-left: -100px;            /* trækker ud til venstre */
    margin-right: -100px;           /* trækker ud til højre */
    background-color: var(--primaer-clr);
  }

  @media (max-width: 800px) {
    .section-intro__line {
      width: calc(100% + 80px);    /* 40px + 40px */
      margin-left: -40px;
      margin-right: -40px;
    }
  }



  .section-intro__heading {
  text-transform: uppercase;
  margin: 0;
  opacity: 0;
  transform: translateY(20px);
  animation: fadeInUp 1s ease forwards;
}

@keyframes fadeInUp {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}



.section-intro__line {
  margin-top: 96px;
  height: 1px;
  width: 0;
  margin-left: 0;
  background-color: var(--primaer-clr);
  animation: drawLine 1s ease forwards 0.5s;
}

@keyframes drawLine {
  to {
    width: calc(100% + 200px);
    margin-left: -100px;
  }
}

@media (max-width: 800px) {
  .section-intro__line {
    animation: drawLineMobile 1s ease forwards 0.5s;
  }
}

@keyframes drawLineMobile {
  to {
    width: calc(100% + 80px);
    margin-left: -40px;
  }
}

</style>

<section class="section-intro">
  <h2 class="section-intro__heading">{text}</h2>
  <div class="section-intro__line"></div>
</section>


