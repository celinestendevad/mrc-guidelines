<section class="sendrequest-strip">
  <div class="sendrequest-inner">
    <div class="sendrequest-content">
      <h3 class="sendrequest-heading">
        Please reach out if you have any ideas you’d like to share
      </h3>

      <a href="mailto:mrc@mikkeller.com" class="sendrequest-button">
        <span>SEND REQUEST</span>
        <img src="/img/mail.svg" alt="" class="sendrequest-icon" />
      </a>
    </div>

    <img
      src="/img/member.grafik.png"
      alt="MRC member illustration"
      class="sendrequest-graphic"
    />
  </div>
</section>

<style>.sendrequest-strip {
  background-color: var(--box-grey-clr);

  margin-left: calc(var(--page-pad) * -1);
  margin-right: calc(var(--page-pad) * -1);

  padding: 72px var(--page-pad) 40px;
  min-height: 200px;
  position: relative;

  margin-bottom: -70px;
}

.sendrequest-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 2rem;
}

.sendrequest-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

.sendrequest-button {
  display: inline-flex;
  align-items: center;
  gap: 10px;

  padding: 12px 32px;
  border-radius: 8px;

  font-family: "Cubano", sans-serif;
  font-size: 16px;
  text-transform: uppercase;
  letter-spacing: 0.08em;

  background: #214082;
  color: white;
  text-decoration: none;
  box-shadow: 0 4px 4px rgba(0,0,0,0.25);
  transition: all 0.2s ease;
}

.sendrequest-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 10px rgba(0,0,0,0.25);
}

.sendrequest-graphic {
  position: absolute;
  right: 40px;
  bottom: -8px;
  height: 150px;
  animation: floaty 2s ease-in-out infinite;
}

@keyframes floaty {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-6px); }
}


@media (max-width: 768px) {
  .sendrequest-strip {
    margin: 0 -0;
    padding: 40px 20px;
    min-height: auto;
  }

  .sendrequest-inner {
    flex-direction: column;
    text-align: center;
  }

  .sendrequest-content {
    align-items: center;
  }

  .sendrequest-button {
    margin: 0 auto;
  }

  .sendrequest-graphic {
    position: static;
    margin-top: 1.5rem;
    height: 120px;
  }
}
</style>